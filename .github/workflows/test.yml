name: Test

on:
  pull_request:
    branches: [main]
  workflow_call:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r app/requirements.txt

      - name: Run tests
        run: |
          echo "TODO: pytest app/tests/ -v"
          echo "Would validate: GET / returns 200 + Hello World"
          echo "Would validate: GET /health returns 200 + JSON status ok"
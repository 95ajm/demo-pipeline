apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demo-pipeline.fullname" . }}-test-connection"
  labels:
    {{- include "demo-pipeline.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demo-pipeline.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
